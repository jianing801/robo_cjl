# robo_cjl 启动命令说明

本文档适用于当前 Windows 环境：

- 项目目录：`C:\Users\Admin\Desktop\DL-BO_code\robo_cjl`
- Python 环境：`C:\Roboparty\env_isaaclab`
- 显卡：NVIDIA GeForce RTX 3060 12GB
- 仿真环境：Isaac Sim 5.1 + Isaac Lab

## 1. 打开 VS Code 终端

在 VS Code 中按 `Ctrl + \`` 打开 PowerShell 终端，然后进入项目目录：

```powershell
Set-Location C:\Users\Admin\Desktop\DL-BO_code\robo_cjl
```

推荐直接使用虚拟环境中 Python 的完整路径。这样不需要 Conda，也不依赖终端是否成功激活环境：

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe --version
```

也可以先激活虚拟环境：

```powershell
& C:\Roboparty\env_isaaclab\Scripts\Activate.ps1
```

激活后可用以下命令确认解释器：

```powershell
python -c "import sys; print(sys.executable)"
```

正确输出应为：

```text
C:\Roboparty\env_isaaclab\Scripts\python.exe
```

> VS Code 已在 `.vscode/settings.json` 中固定使用这个解释器，通常会自动激活。若 PowerShell 禁止执行 `Activate.ps1`，继续使用 Python 完整路径即可，不需要修改系统安全策略。

## 2. 首次启动：参数化结构训练冒烟测试

建议第一次先运行下面这条命令：

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe .\robolab\scripts\tools\co_design_train.py --urdf-model=sw --thigh=0.25 --calf=0.30 --task=RPO-Flat --headless --num-envs=16 --max-iterations=1 --seed=42
```

这条命令只训练 1 轮，用来检查 Python、CUDA、Isaac Sim、参数化 URDF 和 PPO 是否都能运行。看到类似下面的输出即表示成功：

```text
Number of environments: 16
Learning iteration 0/1
CO_DESIGN_CKPT: ...\model_0.pt
CO_DESIGN_LOG_DIR: ...\logs\rsl_rl\rpo_flat\...
```

### 参数逐项解释

| 参数 | 当前示例 | 含义 | 可选值或建议 |
|---|---:|---|---|
| `python.exe` | 环境完整路径 | 使用哪个 Python 环境执行代码 | 固定使用 `C:\Roboparty\env_isaaclab\Scripts\python.exe` |
| `co_design_train.py` | 脚本路径 | 对一组指定腿长生成 URDF 并训练 PPO | 用于单个结构训练，不运行外层贝叶斯优化 |
| `--urdf-model` | `sw` | URDF 物理参数更新模型 | `sw`：使用拟合模型预测质量、质心和惯量；`legacy`：旧比例缩放方法。推荐 `sw` |
| `--thigh` | `0.25` | 大腿长度，单位米 | 必填；当前外层搜索范围为 `0.25～0.40` |
| `--calf` | `0.30` | 小腿长度，单位米 | 必填；当前外层搜索范围为 `0.30～0.45` |
| `--task` | `RPO-Flat` | 训练任务 | 协同设计当前推荐且默认使用 `RPO-Flat` |
| `--headless` | 无参数值 | 不打开仿真窗口，后台训练 | 正式训练建议保留；想看窗口时删除它 |
| `--num-envs` | `16` | GPU 上并行运行的机器人数量 | 冒烟测试 `16`；RTX 3060 建议正式训练从 `256` 或 `512` 开始；显存不足时降低 |
| `--max-iterations` | `1` | PPO 参数更新轮数 | 冒烟测试 `1～10`；短实验 `500～1000`；正式实验常用 `3000`；原研究终训使用 `12000` |
| `--seed` | `42` | 随机种子 | 任意整数；对比实验应保持相同，例如 `42` |
| `--device` | 默认 `cuda:0` | 运行设备 | `cuda:0`、`cuda:1` 或 `cpu`；本机使用 `cuda:0` |

### 可选训练参数

| 参数 | 作用 | 示例 |
|---|---|---|
| `--video` | 训练期间录制视频，同时会启用摄像头 | `--video` |
| `--video-length` | 每段视频的仿真步数 | `--video-length=200` |
| `--video-interval` | 每隔多少训练轮触发录制 | `--video-interval=2000` |
| `--distributed` | 使用多 GPU/多进程训练 | 本机单张 RTX 3060 不要添加 |
| `--early-check` | 在指定轮数检查平均奖励 | `--early-check=500`；`0` 表示关闭 |
| `--early-threshold` | 检查时低于该平均奖励则提前停止 | `--early-threshold=0` |
| `--run_name` | 给日志目录添加易识别的名称 | `--run_name=thigh025_calf030` |
| `--resume` | 从已有训练继续 | 需要同时正确指定 `--load_run` 和 `--checkpoint` |
| `--load_run` | 要恢复的日志运行目录名称或匹配规则 | 例如某次运行的时间目录 |
| `--checkpoint` | 要恢复的模型文件名或匹配规则 | 例如 `model_2999.pt` |

`co_design_train.py` 会强制使用 TensorBoard，避免后台运行时要求登录 WandB。因此即使传入其他 `--logger`，当前代码也会改为 TensorBoard。

### RTX 3060 单结构正式训练示例

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe .\robolab\scripts\tools\co_design_train.py --urdf-model=sw --thigh=0.25 --calf=0.30 --task=RPO-Flat --headless --num-envs=512 --max-iterations=3000 --seed=42 --run_name=baseline_025_030
```

如果出现 CUDA out of memory，将 `--num-envs=512` 降为 `256`、`128` 或 `64`。改变并行环境数会影响采样规模和训练速度，正式对比不同腿长时应使用相同设置。

## 3. 启动双目标结构优化

外层入口是 `co_design.py`。它会自动重复执行以下流程：

1. Optuna 的高斯过程代理模型选择大腿和小腿长度。
2. 根据腿长生成新的 URDF。
3. 为该结构从头训练 PPO。
4. 运行确定性评估。
5. 把速度跟踪代价和机械 CoT 返回给 Optuna。
6. 继续选择下一组结构，最后输出 Pareto 前沿。

### 最小完整流程测试

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe .\robolab\scripts\tools\co_design.py --trials=1 --max-iterations=10 --num-envs=64 --eval-episodes=1 --eval-commands="0.5,0,0" --study-name=rpo_smoke --db=.\experiments\rpo_smoke.db
```

这条命令会测试“选腿长 → 生成 URDF → 训练 → 评估 → 写入数据库”的完整闭环。10 轮训练不足以产生可用策略，结果只能用于检查程序，不代表结构优劣。

### RTX 3060 正式优化示例

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe .\robolab\scripts\tools\co_design.py --trials=30 --max-iterations=3000 --num-envs=512 --eval-episodes=1 --eval-commands="0.5,0,0" --n-startup-trials=5 --seed=42 --study-name=rpo_track_cot_3060 --db=.\experiments\rpo_track_cot_3060.db
```

### 多目标优化参数逐项解释

| 参数 | 默认值 | 含义 | 可选值或建议 |
|---|---:|---|---|
| `--trials` | `30` | 数据库中希望最终拥有的完整 trial 总数 | 正整数。它是“目标总数”，不是每次启动额外增加的数量 |
| `--max-iterations` | `12000` | 每一组腿长内部训练的 PPO 轮数 | 测试 `10`；阶段实验 `1000～3000`；充分训练可用 `12000` |
| `--num-envs` | `4096` | 每个 trial 的并行环境数 | 默认值面向高端多卡机器；RTX 3060 建议 `256～512` |
| `--eval-episodes` | `1` | 每条速度指令评估多少个完整回合 | 快速实验 `1`；更稳定的结果可用 `3～10`，耗时相应增加 |
| `--eval-commands` | `0.5,0,0` | 评估的速度指令，格式为 `vx,vy,wz` | 多条指令使用分号分隔，PowerShell 中必须加引号 |
| `--study-name` | `rpo_flat_track_cot_sw` | Optuna study 名称 | 同一实验恢复时保持不变；新实验使用新名称 |
| `--db` | 脚本目录下的 `co_design_track_cot.db` | 保存 trial、指标和恢复状态的 SQLite 数据库 | 建议显式放在 `.\experiments\名称.db` |
| `--n-startup-trials` | `5` | 高斯过程开始工作前的初始探索 trial 数 | 一般 `5～15`；总 trial 很少时不能大于实际预算太多 |
| `--seed` | `42` | Optuna、训练和评估使用的随机种子 | 任意整数；复现实验应固定 |
| `--train-timeout` | `86400` | 单个结构训练最大时间，单位秒 | 默认 24 小时；超时会被判为不可行结构 |
| `--eval-timeout` | `7200` | 单个结构评估最大时间，单位秒 | 默认 2 小时 |
| `--fail-tracking` | `1000000` | 训练崩溃、超时或指标非法时使用的跟踪惩罚 | 必须是有限正数，通常保留默认值 |
| `--fail-cot` | `1000000` | 不可行结构使用的 CoT 惩罚 | 必须是有限正数，通常保留默认值 |

优化目标固定为：

```text
最小化 tracking_cost
最小化 mechanical CoT
```

它会得到一组 Pareto 最优设计，而不是自动产生唯一冠军。

### 速度指令的写法

速度指令格式：

```text
vx,vy,wz
```

- `vx`：前后速度，正数前进、负数后退，单位 m/s。
- `vy`：左右平移速度，单位 m/s。
- `wz`：绕竖直轴旋转速度，单位 rad/s。

单一前进速度：

```powershell
--eval-commands="0.5,0,0"
```

同时评估慢速、快速和后退：

```powershell
--eval-commands="0.5,0,0;1.0,0,0;-0.5,0,0"
```

同时评估前进和侧移：

```powershell
--eval-commands="0.5,0,0;0,0.5,0"
```

多条指令会增加每个 trial 的评估时间。当前 CoT 目标要求每条指令包含水平移动，因此不要在协同优化里使用纯旋转指令 `0,0,1.0`。

## 4. 恢复或扩展已有优化

多目标优化状态保存在 `--db` 指定的数据库中。按 `Ctrl+C` 中断后，使用完全相同的 `--study-name`、`--db`、URDF 模型、搜索范围和评估协议重新运行即可恢复。

例如已有 12 个完整 trial，希望最终扩展到 30 个：

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe .\robolab\scripts\tools\co_design.py --trials=30 --max-iterations=3000 --num-envs=512 --eval-episodes=1 --eval-commands="0.5,0,0" --n-startup-trials=5 --seed=42 --study-name=rpo_track_cot_3060 --db=.\experiments\rpo_track_cot_3060.db
```

程序会读取数据库，只补充到 30 个完整 trial，不会重新执行已经完成的 trial。

如果修改了评估速度、腿长搜索范围或模型协议，应使用新的 `--study-name` 和新的数据库文件，避免混合不同实验条件。

## 5. 普通 RPO 训练

如果不改变腿长，只训练仓库中的默认机器人，可运行：

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe .\robolab\scripts\rsl_rl\train.py --task=RPO-Flat --headless --num_envs=512 --max_iterations=3000 --logger=tensorboard --seed=42
```

注意普通训练脚本使用下划线参数：

```text
--num_envs
--max_iterations
```

而协同设计脚本使用连字符：

```text
--num-envs
--max-iterations
```

不要混用。

仓库中注册的任务包括：

- `RPO-Flat`
- `RPO-Rough`
- `RPO-AttnEnc`
- `RPO-Interrupt`
- `RPO-Parkour`
- `RPO-Parkour-Play`
- `RPO-BeyondMimic`
- `RPO-Getup-Mimic`
- `RPO-AMP`
- `RPO-AMP-Play`

结构协同设计代码目前围绕 `RPO-Flat` 编写。其他任务可能需要运动数据、额外配置或专门训练入口，不建议直接替换 `co_design.py` 中的任务名称。

## 6. 查看训练结果

训练模型和 TensorBoard 日志默认保存在：

```text
C:\Users\Admin\Desktop\DL-BO_code\robo_cjl\logs\rsl_rl\rpo_flat\<运行时间>\
```

目录内通常包含：

- `model_*.pt`：策略 checkpoint。
- TensorBoard event 文件：训练奖励、损失等曲线。
- `params`：本次训练配置。
- `git`：启动训练时记录的代码差异。

在 VS Code 终端启动 TensorBoard：

```powershell
& C:\Roboparty\env_isaaclab\Scripts\python.exe -m tensorboard.main --logdir .\logs\rsl_rl --port 6006
```

然后在浏览器打开：

```text
http://localhost:6006
```

多目标优化数据库位于 `--db` 指定的位置。优化结束后还会生成同名的 Pareto CSV，例如：

```text
experiments\rpo_track_cot_3060.db
experiments\rpo_track_cot_3060_pareto.csv
```

## 7. 在 VS Code 中不用输入命令启动

按 `Ctrl+Shift+D` 打开“运行和调试”，可以选择已经配置好的入口：

- `01 普通训练冒烟测试（16环境/1轮）`
- `02 结构参数训练冒烟测试（16环境/1轮）`
- `03 正式普通训练（512环境/3000轮）`
- `04 评估指定模型`

选择后按 `F5`。需要修改腿长、环境数或训练轮数时，编辑 `.vscode/launch.json` 中对应入口的 `args`。

## 8. 推荐执行顺序

第一次使用建议依次执行：

1. 参数化结构训练冒烟测试：16 环境、1 轮。
2. 完整优化闭环测试：1 trial、10 轮、64 环境。
3. 单结构短训练：256 或 512 环境、500～1000 轮。
4. 确认显存、速度和奖励正常后，再运行 3000 轮和多个 trial。

不要一开始就运行默认的 `30 × 12000 × 4096` 配置。该默认值面向高端多 GPU 机器，在单张 RTX 3060 上耗时极长，也很可能显存不足。
